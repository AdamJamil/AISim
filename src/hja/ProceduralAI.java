package hja;

public class ProceduralAI implements Constants
{
    private Checker checker = new Checker();
    private int player = x;

    public Position getMove(Data data)
    {
        if (checker.getMoveToWin(player, data).x != -1)

    }
}
